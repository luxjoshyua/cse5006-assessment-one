dashboard live update
curl -s localhost:3000/api/items > /dev/null

health check
curl -s localhost:3000/api/health | json_pp

metrics
curl -s localhost:3000/api/metrics | json_pp | head -40

playwright
pnpm test:e2e

404 does not exist
curl -s localhost:3000/api/items/does-not-exist | json_pp